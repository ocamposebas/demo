---
export const prerender = false;
import BaseLayout from "../layouts/BaseLayout.astro";
import ResourceExperience from "../components/shop/ResourceExperience.jsx";
const canonical = new URL(Astro.url.pathname, Astro.url.origin).toString();
---
<BaseLayout title="Preguntas frecuentes | LAB_CORE" description="Respuestas sobre pedidos, certificados de análisis, seguimiento, pagos y uso responsable de LAB_CORE." canonical={canonical}><ResourceExperience page="faqs" client:idle /></BaseLayout>
